---
permalink: search.json
---
[
{%- for post in collections.post -%}
  {
    "url": "{{ post.url }}",
    "title": "{{ post.data.title }}",
    "content": "{{ post.templateContent | squash }}"
  }{% if not loop.last %},{% endif %}
{%- endfor -%}
]
